name: sherin
on:
  schedule:
    - cron:  '0 0 * * 5,7'
env:
  GH_TOKEN: ${{secrets.GH_TOKEN}}
jobs:
  kadin_job:
    uses: ./.github/workflows/reusable.yml
    with:
      GENDER: sponsor
      maxconcurrency: '5'
      WEBSITE: 'biraradamoda'
      marka: 'sherin'
    secrets: inherit
